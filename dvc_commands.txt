Commands while using DVC

git init
dvc init
mkdir data                  (to create new folder)<make directory>

dvc add data/train.csv      (coverting unstaged to staged)
git add data/.gitignore data/train.csv.dvc
nano data/.gitignore        (to check if it got ignored or not) (not required)

git config user.name 'datamites'
git config user.email 'dat@gmail.com'

(now create a folder called DVC in your google drive)

dvc remote add -d storage gdrive://<copy paste the numbers on the link on top of the gdrive>

git status
git add .dvc/config
git status

(now create a new repository in github and copy paste the origin link)
git remote add origin https://github.com/shreelakshmi-22/DVC-19.git

dvc push

git commit -m "first version"
git push origin master
